import { EntityFormSkeleton } from '@/components/common/entity-edit-skeleton';

/** Suspense boundary for this segment - see components/common/entity-edit-skeleton. */
export default function NewUsersLoading() {
  return <EntityFormSkeleton />;
}
