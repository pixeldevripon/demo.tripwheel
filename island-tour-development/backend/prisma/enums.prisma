// All project-wide enums live here.

// ── User / Auth ───────────────────────────────────────────────────────────────

enum Role {
  ADMIN
  EDITOR
  STAFF
  GUIDE
  TOUR_OPERATOR
  USER
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  DELETED
}

// ── Permissions (used in ROLE_PERMISSIONS map — not stored on the User row) ───
// Full list from legacy RBAC + Monorepo new requirements

enum Permission {
  // Monorepo specific / System
  VIEW_PERMISSIONS
  MANAGE_SYSTEM
  MANAGE_TRIPS
  MANAGE_SLOTS
  VIEW_SLOT_ANALYTICS
  CREATE_CONTENT
  VIEW_CONTENT
  EDIT_CONTENT
  DELETE_CONTENT
  VIEW_MEDIA
  VIEW_ORDERS
  EDIT_ORDER
  DELETE_ORDER

  // Users
  MANAGE_USERS
  VIEW_USERS
  CREATE_USER
  UPDATE_USER
  DELETE_USER

  // Trips (Legacy compatibility)
  CREATE_TRIP
  VIEW_TRIPS
  EDIT_TRIP
  DELETE_TRIP

  // Blogs
  CREATE_BLOG
  VIEW_BLOGS
  EDIT_BLOG
  DELETE_BLOG

  // Destinations
  CREATE_DESTINATION
  VIEW_DESTINATIONS
  EDIT_DESTINATION
  DELETE_DESTINATION

  // Activities
  CREATE_ACTIVITY
  VIEW_ACTIVITIES
  EDIT_ACTIVITY
  DELETE_ACTIVITY

  // Pickups & Drops
  CREATE_PICKUP_DROP
  VIEW_PICKUP_DROPS
  EDIT_PICKUP_DROP
  DELETE_PICKUP_DROP

  // Categories
  CREATE_CATEGORY
  VIEW_CATEGORIES
  EDIT_CATEGORY
  DELETE_CATEGORY

  // Bookings
  VIEW_BOOKINGS
  EDIT_BOOKING
  DELETE_BOOKING

  // Payments
  VIEW_PAYMENTS
  EDIT_PAYMENT
  DELETE_PAYMENT

  // Enquiries
  VIEW_ENQUIRIES
  DELETE_ENQUIRY
  REPLY_ENQUIRY

  // Leads
  VIEW_LEADS
  EDIT_LEAD
  DELETE_LEAD

  // Reviews
  VIEW_REVIEWS
  EDIT_REVIEW
  DELETE_REVIEW

  // Partners
  CREATE_PARTNER
  VIEW_PARTNERS
  EDIT_PARTNER
  DELETE_PARTNER

  // Files & Media
  UPLOAD_MEDIA
  MANAGE_MEDIA

  // Profile
  VIEW_PROFILE
  EDIT_PROFILE

  // Settings
  MANAGE_SETTINGS
  VIEW_SETTINGS

  // Analytics & Data
  VIEW_ANALYTICS
  EXPORT_DATA
  BULK_OPERATIONS
}

// ── Trip ──────────────────────────────────────────────────────────────────────

enum TripStatus {
  DRAFT
  PENDING_REVIEW
  LIVE
  PAUSED
  ARCHIVED
}

// ── Featured Slots ────────────────────────────────────────────────────────────

enum SlotStatus {
  AVAILABLE
  SOFT_LOCKED
  HARD_RESERVED
}

// ── Waitlist ──────────────────────────────────────────────────────────────────

enum WaitlistStatus {
  WAITING
  OFFERED
  CLAIMED
  PASSED
  EXPIRED
  CANCELLED
}

// ── Bookings ──────────────────────────────────────────────────────────────────

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  COMPLETED
  REFUNDED
}

// ── Operator ──────────────────────────────────────────────────────────────────

enum OperatorVerificationStatus {
  UNVERIFIED
  PENDING
  VERIFIED
  REJECTED
}
